// backend/.sequelizerc
const path = require('path');

// this is the configuration we see in the bd folder
// This file helps to create the folder structure and where items need to be cretated for the model, seeder and migration files
module.exports = {
  config: path.resolve('config', 'database.js'),
  'models-path': path.resolve('db', 'models'),
  'seeders-path': path.resolve('db', 'seeders'),
  'migrations-path': path.resolve('db', 'migrations')
};

// The command to initialize the db folder structure is ( npx sequelize init )
